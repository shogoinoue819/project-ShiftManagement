{
  "name": "shift-management-gas",
  "version": "1.0.0",
  "description": "Google Apps Script シフト管理システム",
  "scripts": {
    "push": "echo '⚠️  環境を指定してください: npm run push:prod または npm run push:test'",
    "push:prod": "echo '📤 本番環境用にビルド中...' && node scripts/build-env.js production && echo '📤 本番環境にpush中...' && clasp push --project .clasp.json",
    "push:test": "echo '🧪 テスト環境用にビルド中...' && node scripts/build-env.js test && echo '🧪 テスト環境にpush中...' && clasp push --project .clasp-test.json",
    "build:prod": "node scripts/build-env.js production",
    "build:test": "node scripts/build-env.js test",
    "status": "echo '📋 現在の設定:' && echo '本番: ' && cat .clasp.json && echo 'テスト: ' && cat .clasp-test.json",
    "config:show": "echo '⚙️  現在のconsts-env.jsの内容:' && head -20 consts-env.js",
    "setup": "echo '🔧 セットアップ手順:' && echo '1. config/env-config.example.json を config/env-config.json にコピー' && echo '2. config/env-config.json で実際のIDを設定' && echo '3. npm run push:test または npm run push:prod でpush'"
  },
  "keywords": [
    "google-apps-script",
    "shift-management"
  ],
  "author": "",
  "license": "MIT"
}
